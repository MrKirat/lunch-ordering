%table.table.table-dark.table-bordered.mt-3
  %thead
    %tr.text-center
      %th Name
      %th Price
      %th Image

  %tbody
    - items.each do |item|
      %tr.text-center
        %td= item.name
        %td= item.price.format
        %td= image_tag item.image, class: 'img-fluid img-thumbnail' if item.image.attached?