%table.table
  %thead
    %tr
      %th Name
      %th Price

  %tbody
    - @order.food_items.each do |item|
      %tr
        %th= item.name
        %th= item.price.format

%p
  %b Total price:
  %span= @order.total_cost.format

= link_to 'Back', orders_path
