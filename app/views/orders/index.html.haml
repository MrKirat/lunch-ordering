%h1 Listing orders

.mb-3= link_to 'New Order', new_order_path

%table.table
  %thead
    %tr
      %th Date
      %th

  %tbody
    - @orders.each do |order|
      %tr
        %td= order.created_at
        %td= link_to 'Show', order