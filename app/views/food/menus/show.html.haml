%p
  %b Date:
  = menu_date(@menu)
- @menu.categories.each do |category|
  %h3= category.name

  %table.table
    %thead
      %tr
        %th Name
        %th Price

    %tbody
      - category.items.each do |item|
        %tr
          %th= item.name
          %th= item.price

= link_to 'Back', :back
