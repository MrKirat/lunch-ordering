%h2.text-center Forgot your password?
= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: {class: "devise-form ml-auto mr-auto", method: :post}) do |f|
  = f.error_notification
  = f.input :email, input_html: { autocomplete: 'email', autofocus: true }
  = f.button :submit, "Send me reset password instructions", class: "btn-primary text-center"
.text-center
  = render "users/shared/links"
