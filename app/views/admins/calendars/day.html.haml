%h1 Daily stats

= link_to 'Prev', admins_calendars_day_path(:start_date => (Time.zone.parse(@date) - 1.day).strftime('%Y-%m-%d'))
%span= @date
= link_to 'Next', admins_calendars_day_path(:start_date => (Time.zone.parse(@date) + 1.day).strftime('%Y-%m-%d'))

%h3 Menus

= render 'admins/food_menus/collection', menus: @menus

%h3 Orders

%b Total cost per day:
= @orders.reduce(Money.new(0)) { |sum, order| sum + order.total_cost }

= render 'admins/orders/collection', orders: @orders
